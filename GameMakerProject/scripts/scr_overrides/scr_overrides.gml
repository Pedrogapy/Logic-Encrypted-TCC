function game_end() {
    // Esta função foi intencionalmente deixada em branco.
    // Ao recriar a função game_end(), nós impedimos que o comportamento
    // padrão do GameMaker (fechar o jogo com a tecla Esc) seja executado.
    // Agora, apenas nosso código de menu de pausa controlará a tecla Esc.
    
    show_debug_message("Ação padrão de fechar o jogo foi bloqueada com sucesso.");
}